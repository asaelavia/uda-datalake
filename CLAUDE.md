# CLAUDE.md — Unsupervised Domain Adaptation in Data Lakes

## Problem Statement

**Input:** A true data lake (GitTables — ~1M unlabeled CSV tables from GitHub) + a target table (unlabeled, needs predictions).
**Output:** Predictions for the target table, using labeled sources extracted from the lake via source repurposing.

**Key constraint:** No external labeled datasets. All labeled signal must come from the lake itself via source repurposing (finding columns whose names semantically match the target label).

---

## Pipeline

```
INPUT: GitTables lake (~1M tables, all unlabeled) + unlabeled target table
         │
         ▼
STEP 0: SOURCE REPURPOSING
         │  scan all lake tables → find columns semantically matching target label
         │  → repurposed tables become labeled sources (feature col = pseudo-label)
         ▼
STEP 1: TABLE DISCOVERY
         │  embed columns → cosine similarity + bipartite matching → ranked scores
         ▼
STEP 2: SCHEMA ALIGNMENT
         │  Hungarian matching → aligned DataFrames per source
         ▼
STEP 3: WEIGHTED MULTI-SOURCE ADAPTATION
         │  Baseline / Level 0 / Level 2 / Level 5 / Level 5.5 / Level 6
         │  Non-repurposed lake tables used as unlabeled domain data in L5/L5.5/L6
         ▼
STEP 4: EVALUATION
         └─ compare levels + naive baseline → show negative transfer
```

One module per step. Do not merge steps.

---

## Step Details

### Step 0 — Source Repurposing (`table_discovery.py`, `_stream_load_and_repurpose` + `_build_labeled_lake` in `act5_gittables_lake.py`)
- Scan all GitTables lake tables for columns whose name is semantically similar to the target label
- Concept expansion: LLM (Ollama/Qwen3.5) → KG (DBpedia fallback) → label-only fallback
- **Critical distinction:** concepts must be synonyms/proxy labels (columns that COULD BE the label), NOT predictive features. E.g. for "income": salary, wages ✓ — occupation, education ✗
- Concepts cached in `data/llm_expansion_cache.json` (pre-populated for all 14 targets via Claude Opus)
- Repurposing threshold: **0.70** (with expanded concept lists; 0.35 matches too many tables)
- **Direction-aware binarization:** proxy column polarity looked up in `data/direction_cache.json`; columns like `active` (NEGATIVE for churn) are flipped so higher value always means more of target. Generated by `generate_direction_cache.py` using Ollama.
- **Otsu threshold:** binarization threshold chosen by Otsu's method (minimises within-class variance) instead of fixed median. Fallback to median only for degenerate columns (<10 rows or constant).
- **SANTOS neighbor filter:** rejects repurposed tables where the columns adjacent to the proxy column have low cosine similarity to the target feature centroid — filters wrong-domain tables (e.g. code-complexity tables that happen to contain a "churn" column). Applied at both streaming scan and done-cache load. (`NEIGHBOR_CONTEXT_THRESHOLD = 0.12`)
- **Context filter:** `CONTEXT_THRESHOLD = 0.20` (table-level centroid vs target feature centroid)
- Streaming single-pass: reads each parquet, checks threshold, discards non-matches immediately
- Checkpointed every 5% to `data/stream_ckpt_*.json` — safe to pause/resume
- **Diagnostics:** every run writes `results/act5/{target}/diagnostics/step0_repurposed.csv` (per-table direction, threshold, pos_rate, kept/rejected)
- **Output:** subset of lake tables promoted to labeled sources, with assigned label column

### Step 1 — Table Discovery (`table_discovery.py`)
- Embed column names with `sentence-transformers` (frozen, no training)
- Compute pairwise table similarity: cosine similarity + bipartite matching score
- Rank lake tables by relevance to the target
- TOP_K = **20** tables selected for adaptation
- **Diagnostics:** every run writes `results/act5/{target}/diagnostics/step1_discovery.csv`
- **Output:** `dict[table_id → similarity_score]`

### Step 2 — Schema Alignment (`schema_alignment.py`)
- For each relevant source table, map its columns to target columns via Hungarian algorithm (`scipy.optimize.linear_sum_assignment`)
- Drop unmatched columns, rename matched ones to target schema
- **Diagnostics:** every run writes `results/act5/{target}/diagnostics/step2_alignment.csv`
- **Output:** aligned `DataFrame` per source table

### Step 3 — Weighted Multi-Source Adaptation (`domain_adaptation.py`)

| Level | Description |
|---|---|
| **Baseline** | Naive equal-weight combination of all sources (demonstrates negative transfer) |
| **Level 0** | Direct transfer from single best source (highest discovery score) |
| **Level 2** | Weighted multi-source XGBoost + pseudo-labeling: iterative self-training with high-confidence target rows. Quality gate: skips pseudo-labeling when n_target < 100. |
| **Level 2+LSC** | L2 variant with label shift correction — calibrates predicted positive rate toward target. Helps on moderate shift; hurts on extreme shift. |
| **Level 5** | DANN adversarial adaptation; unlabeled lake tables (target side) + volume sources (source side) augment discriminator |
| **Level 5.5** | CDAN (conditional DANN) — falls back to DANN when fewer than 200 source rows |
| **Level 6** | FTTA (Feature-Transform TTA): CDO + LCW + DME; test-time adaptation anchored on L5 |

Removed from pipeline: L1 (weighted XGBoost, subsumed by L2), L3/L4 (instance reweighting, marginal gain), level_ot (OT reweighting — covariate shift assumption violated by proxy label concept drift), level_qbc (QBC committee filter — net negative across targets, only helped noshow).

- **Output:** predictions for the target table, one set per level

### Step 4 — Evaluation (`evaluation.py`)
- Compare all levels and the naive baseline
- Primary result: equal-weight baseline hurts (negative transfer); discovery-weighted approach prevents it
- **Output:** metrics table (accuracy / F1 / AUC) across levels, both default and calibrated thresholds

---

## Data Lake

**Primary lake: GitTables** (https://gittables.github.io)
- ~1M CSV tables extracted from GitHub repositories
- Stored locally as parquet files in `data/gittables/` with `manifest.json`
- Downloaded from Zenodo record 6517052 via `python gittables_lake.py --download-zenodo`
- Also includes ~9,807 tables from `target-benchmark/gittables-corpus` (HuggingFace)
- **All tables are unlabeled** — labeled signal extracted only via source repurposing
- GPU-accelerated repurposing scan: RTX 4060, batch_size=512 tables per encode call

**Previous lake (completed, for reference):** OpenML 787-lake
- 705 labeled binary classification datasets from OpenML
- Run via `act4_openml_lake.py`
- Clean results (contamination fixed): oracle > adapted for all 5 targets

---

## Experimental Targets

### All 14 targets (act5, results available)

| Target | OpenML ID | Label | Sources (quality) | Best discovery score | Notes |
|---|---|---|---|---|---|
| adult | 1590 | income >$50k | 20 | 0.630 | Best repurposing signal |
| diabetes | 37 | diabetes positive | 20 | 0.345 | bmi/obesity/waist_cm added to concepts |
| churn | 42178 | customer churn | 20 | 0.410 | Strong signal; direction fix removes 54% inverted-polarity sources |
| credit | 31 | credit good/bad | 20 | 0.544 | Moderate signal |
| bank | 1461 | term deposit | 4 | 0.212 | Hard — weak discovery scores, min_sim fallback |
| heart | 53 | heart disease | 3 | 0.382 | Only 3 repurposed sources; ICU domain mismatch — all adapted methods below baseline |
| nyhouse | — | price >$1M | 2+vol | 0.544 | min_cols fallback + volume augmentation |
| turnover | 43551 | employee turnover | 20 | 0.356 | Extreme imbalance (1.4% positive), all methods ≈ 0.5 |
| crime | 2116 | violent crime high | 20 | 0.537 | Good signal, 53% gap |
| obesity | — | county obesity high | 20 | 0.510 | CDC data, 43% gap |
| noshow | — | medical no-show | 20 | 0.398 | L5 best; direction cache intentionally empty — GitTables "attend"/"is_show" columns are non-medical; flipping polarity hurts (tested, dropped L5 to 0.456) |
| titanic | 40945 | passenger survival | 20 | 0.564 | 51% gap (L5.5=0.761 after CDAN threshold fix 1000→200) |
| stroke | — | stroke diagnosis | 20 | 0.421 | 58% gap, L2 best |
| breastcancer | 1510 | malignant | 0 | 0.017 | FAILED: max discovery score < threshold; clinical measurements don't appear in repurposed sources |

---

## Current Results (act5, AUC — calibrated threshold)

Results include: direction-aware binarization (direction oracle via Ollama), Otsu threshold replacing fixed median, SANTOS neighbor filter (NEIGHBOR_CONTEXT_THRESHOLD=0.12), CONTEXT_THRESHOLD raised 0.15→0.20, alignment fix (min_similarity=0.35), min_cols fallback, volume source augmentation for DANN, CDAN threshold lowered 1000→200 rows, L2 pseudo-labeling quality gate (n_target < 100 → skip), L2+LSC label shift correction variant, L6 (FTTA) added.
Removed: L1/L3/L4/level_ot/level_qbc.
Note: L5/L6 (DANN) are stochastic on CUDA and show ±0.05 run-to-run variance.

### AUC by level

| Target | Baseline | L0 | L2 | L2+LSC | L5 | L5.5 | L6 | Oracle |
|---|---|---|---|---|---|---|---|---|
| **adult** | 0.577 | 0.658 | **0.734** | 0.700 | 0.604 | 0.678 | 0.672 | 0.903 |
| **diabetes** | 0.563 | 0.563 | **0.643** | 0.617 | 0.616 | 0.616 | 0.582 | 0.829 |
| **churn** | 0.649 | 0.651 | **0.728** | 0.700 | 0.664 | 0.651 | 0.707 | 0.836 |
| **credit** | 0.464 | 0.439 | 0.419 | 0.408 | 0.502 | **0.561** | 0.508 | 0.756 |
| **bank** | 0.537 | 0.725 | **0.781** | 0.713 | 0.482 | 0.513 | 0.596 | 0.901 |
| **heart** | **0.756** | 0.587 | 0.587 | 0.587 | 0.389 | 0.389 | 0.354 | 0.883 |
| **nyhouse** | 0.562 | 0.684 | **0.758** | 0.663 | 0.652 | 0.658 | 0.652 | 0.918 |
| **turnover** | **0.523** | 0.495 | 0.507 | 0.515 | 0.461 | 0.461 | 0.437 | 0.737 |
| **crime** | 0.431 | 0.570 | **0.684** | 0.617 | 0.357 | 0.391 | 0.443 | 0.911 |
| **obesity** | 0.705 | 0.557 | **0.787** | 0.753 | 0.700 | 0.710 | 0.724 | 0.897 |
| **noshow** | 0.511 | 0.519 | 0.519 | 0.542 | **0.605** | 0.605 | 0.591 | 0.708 |
| **titanic** | 0.638 | 0.653 | 0.690 | 0.664 | 0.638 | **0.761** | 0.638 | 0.880 |
| **stroke** | 0.689 | 0.647 | **0.767** | 0.636 | 0.495 | 0.373 | 0.532 | 0.823 |
| **breastcancer** | — | — | — | — | — | — | — | 0.986 |

### % of oracle gap closed (AUC, relative to baseline)

| Target | L0 | L2 | L5 | L5.5 | Best | Gap% |
|---|---|---|---|---|---|---|
| **adult** | 25% | **48%** | 8% | 31% | L2 | 48% |
| **diabetes** | 0% | **30%** | 20% | 20% | L2 | 30% |
| **churn** | 1% | **42%** | 8% | 1% | L2 | 42% |
| **credit** | -11% | -15% | 13% | **33%** | L5.5 | 33% |
| **bank** | 52% | **67%** | -15% | -6% | L2 | 67% |
| **heart** | neg | neg | neg | neg | baseline | 0% |
| **nyhouse** | 34% | **55%** | 25% | 27% | L2 | 55% |
| **turnover** | neg | neg | neg | neg | baseline | 0% |
| **crime** | 29% | **53%** | neg | neg | L2 | 53% |
| **obesity** | neg | **43%** | neg | 3% | L2 | 43% |
| **noshow** | 4% | 4% | **48%** | 48% | L5 | 48% |
| **titanic** | 6% | 21% | 0% | **51%** | L5.5 | 51% |
| **stroke** | neg | **58%** | neg | neg | L2 | 58% |
| **breastcancer** | — | — | — | — | FAILED | — |

**Average oracle gap (11 successful targets): 48%**

### Key findings
- **L2 (pseudo-labeling XGBoost) wins on 8/11 successful targets** — the dominant method
- **L5.5 (CDAN) wins on 2/11** (credit, titanic) — after CDAN threshold fix (1000→200 rows)
- **L5 (DANN) wins on 1/11** (noshow) — better when class balance is tricky
- **11/13 runnable targets** show adaptation improvement over equal-weight baseline
- **Average oracle gap: 48%** across 11 successful targets
- **Best individual gains:** bank 67%, stroke 58%, nyhouse 55%, crime 53%
- **Heart/turnover:** baseline is optimal — ICU domain mismatch / extreme imbalance (1.4% positive) defeat adaptation
- **Breastcancer:** FAILED — 0 aligned sources; max discovery score 0.017
- **Negative transfer eliminated** for 11/13 targets

### L2+LSC (label shift correction) notes
- Helps on moderate label shift: noshow (+0.023), turnover (+0.008)
- Hurts on extreme shift: stroke (0.767→0.636), adult (0.734→0.700) — calibration loop diverges
- Use L2 as default; L2+LSC is an experimental variant

### Tried and rejected algorithms
| Algorithm | Result | Reason |
|---|---|---|
| OT reweighting (level_ot) | Never best | Covariate shift assumption violated: source proxy labels have different semantics than target labels |
| QBC-F (level_qbc) | 1 win (noshow +0.154), 4 losses | Committee shares same systematic bias as main model; net negative overall |

---

## Source Repurposing — Concept Lists

Concepts are pre-generated by Claude Opus and cached in `data/llm_expansion_cache.json`.
**Rule:** include synonyms + proxy labels (columns that COULD BE the label); exclude predictive features.

| Target label | n_concepts | Example concepts |
|---|---|---|
| income above 50k | 36 | salary, wages, annual_income, household_income, IncomeLevel |
| diabetes diagnosis positive | 47 | diabetes, glucose, hba1c, fasting_glucose, bmi, obesity, waist_cm |
| customer churn | 35 | churn, churned, attrition, cancelled, Exited, subscription_status |
| credit risk good or bad | 35 | credit_risk, default, credit_score, loan_status, good_bad, fico |
| term deposit subscription | 30 | deposit, term_deposit, subscribed, campaign_outcome, conversion |
| heart disease diagnosis | 30 | heart_disease, cad, cvd, troponin, cholesterol, ejection_fraction |
| employee turnover | 30 | turnover, attrition, resigned, terminated, Attrition, LeaveOrNot |
| house price above 1 million | 35 | price, sale_price, property_value, SalePrice, appraised_value |

---

## Contamination Rules

Name patterns excluded from lake (applied at both manifest-build and load time):
- `adult`, `census`, `credit` (substring), `creditability`, `diabetes`, `pima`, `bank`, `marketing`, `fraud`

Note: GitTables lake has no name-based contamination filter in `gittables_lake.py` — only act4 OpenML lake applies these. Contamination for act5 targets is mitigated by the repurposing threshold and discovery scoring.

---

## Technology Stack

| Purpose | Library |
|---|---|
| Data manipulation | `pandas` |
| ML & metrics | `scikit-learn` |
| Column embeddings | `sentence-transformers` |
| Gradient boosting | `xgboost` |
| Hungarian matching | `scipy` |
| Deep learning (Level 5 only) | `torch` (CUDA — RTX 4060) |
| LLM concept expansion + direction oracle | Ollama + Qwen3.5 (local, no API cost) |
| HTTP downloads | `requests` |
| Python version | 3.10+ |

---

## Constraints

- **No neural network training** (except Level 5). `sentence-transformers` is used as a frozen encoder only.
- **No external labeled datasets.** All labeled signal must come from source repurposing within the lake.
- One module per pipeline step — `table_discovery`, `schema_alignment`, `domain_adaptation`, `evaluation`.
- Keep functions pure and stateless; avoid global state.
- Concept expansion must use LLM output — no manual curation of concept lists.

---

## Code Conventions

- Type hints on all function signatures.
- Each module has a small, explicit public API; internals prefixed with `_`.
- `pathlib.Path` for all file paths.
- `logging` module only — no `print` statements.
- Notebooks are exploratory scratch space, not deliverables.

---

## Directory Layout

```
uda-datalake/
├── CLAUDE.md
├── act4_openml_lake.py          # OpenML lake experiment (completed reference)
├── act5_gittables_lake.py       # GitTables lake experiment (main)
├── generate_direction_cache.py  # Generates data/direction_cache.json via Ollama
├── gittables_lake.py            # GitTables downloader + loader
├── table_discovery.py           # Step 0+1 (source repurposing + table discovery)
├── schema_alignment.py          # Step 2
├── domain_adaptation.py         # Step 3
├── evaluation.py                # Step 4
├── data/
│   ├── gittables/               # GitTables parquet cache + manifest.json
│   │   ├── manifest.json
│   │   └── gt_*.parquet
│   ├── llm_expansion_cache.json # Pre-generated concept lists (all 14 targets)
│   ├── direction_cache.json     # Proxy column polarity (POSITIVE/NEGATIVE) per target, 14 targets ~400 entries
│   ├── repurpose_done_*.json    # Done-caches: {table_id → proxy_col} per target scan
│   ├── stream_ckpt_*.json       # Repurposing scan checkpoints (auto-deleted on completion)
│   └── NY-Housing/
├── logs/
│   └── act5_*.log               # Per-target run logs
└── results/
    ├── act4/                    # OpenML lake results per target
    └── act5/                    # GitTables lake results per target
        └── {target}/
            ├── metrics.csv              # Calibrated + uncalibrated AUC/F1/accuracy per level
            ├── metrics_uncalibrated.csv
            ├── transferability.csv      # Per-source transfer scores
            ├── discovery_scores.csv     # Step 1 discovery scores
            └── diagnostics/
                ├── step0_repurposed.csv # Per-table direction, threshold, pos_rate, kept/rejected
                ├── step1_discovery.csv  # Discovery scores with proxy col names
                ├── step2_alignment.csv  # Column mapping per source
                └── source_self_auc.csv  # Source self-AUC quality scores
```

---

## Running the Pipeline

```bash
# Download GitTables lake (resumable, run until complete):
python gittables_lake.py --download-zenodo

# (Re-)generate direction cache for all targets — requires Ollama running locally:
python generate_direction_cache.py --all
# Or for a single target:
python generate_direction_cache.py --targets noshow

# Run GitTables experiment (act5):
python act5_gittables_lake.py --target adult
python act5_gittables_lake.py --target churn

# Run all 14 sequentially:
python act5_gittables_lake.py --target adult
python act5_gittables_lake.py --target diabetes
python act5_gittables_lake.py --target churn
python act5_gittables_lake.py --target credit
python act5_gittables_lake.py --target bank
python act5_gittables_lake.py --target heart
python act5_gittables_lake.py --target nyhouse
python act5_gittables_lake.py --target turnover
python act5_gittables_lake.py --target crime
python act5_gittables_lake.py --target obesity
python act5_gittables_lake.py --target noshow
python act5_gittables_lake.py --target titanic
python act5_gittables_lake.py --target stroke
python act5_gittables_lake.py --target breastcancer

# Run OpenML lake experiment (reference, all targets):
python act4_openml_lake.py --lake-size 787 --target adult
```

---

## Hard Cases — Known Limitations

| Target | Problem | Status |
|---|---|---|
| **heart** | Only 3 repurposed sources; all from ICU mortality domain (≠ outpatient heart disease). Adapted methods (best L2=0.587) below baseline (0.756). | Open — needs broader concept list or domain-specific sources |
| **turnover** | Extreme class imbalance (1.4% positive). All methods predict majority class, AUC ≈ 0.5. | Open — needs SMOTE/cost-sensitive handling beyond current scope |
| **breastcancer** | 0 aligned sources. Max discovery score 0.017. Clinical measurement columns (radius, texture, perimeter) not found in GitTables repurposed sources. | Open — needs concept list revision targeting lab-measurement synonyms |
