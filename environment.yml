name: uda-datalake
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - pandas
    - scikit-learn
    - sentence-transformers
    - xgboost
    - scipy
    - folktables
